I"(function() {
  Storytime.Dashboard.Tags = (function() {
    function Tags() {}

    Tags.prototype.init = function() {
      return $(".tags-input").each(function() {
        var searchInput, tagsInput;
        tagsInput = $(this);
        searchInput = tagsInput.next("div").find(".search-field").children("input");
        return searchInput.on('keyup', function(e) {
          var searched_tag, selected_tags;
          if (e.which === 13 && searchInput.val().length > 0) {
            searched_tag = searchInput.val();
            tagsInput.append('<option value="nv__' + searched_tag + '">' + searched_tag + '</option>');
            selected_tags = tagsInput.val() || [];
            selected_tags.push("nv__" + searched_tag);
            tagsInput.val(selected_tags);
            tagsInput.trigger('chosen:updated');
          }
        });
      });
    };

    return Tags;

  })();

}).call(this);
:ET